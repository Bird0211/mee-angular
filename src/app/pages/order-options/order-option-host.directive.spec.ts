import { OrderOptionHostDirective } from './order-option-host.directive';

describe('OrderOptionHostDirective', () => {
  it('should create an instance', () => {
    const directive = new OrderOptionHostDirective();
    expect(directive).toBeTruthy();
  });
});
