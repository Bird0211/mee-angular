import { InvoiceConfirmData } from './invoice-confirm-data';

describe('InvoiceConfirmData', () => {
  it('should create an instance', () => {
    expect(new InvoiceConfirmData()).toBeTruthy();
  });
});
