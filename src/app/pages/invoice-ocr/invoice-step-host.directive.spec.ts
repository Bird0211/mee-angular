import { InvoiceStepHostDirective } from './invoice-step-host.directive';

describe('InvoiceStepHostDirective', () => {
  it('should create an instance', () => {
    const directive = new InvoiceStepHostDirective();
    expect(directive).toBeTruthy();
  });
});
